# 環境變數範本 — 複製為 backend/.env 後填入實際值(.env 已列入 .gitignore,不得提交)
# 本檔案僅說明需要哪些變數,不得填入真實機密值(AGENTS.md 機密資訊管理)

# 資料庫連線字串
# 本地開發(SQLite,預設值,可不設):sqlite:///./palworld.db
# 正式環境(Render):由 Dockerfile 設為 image 內的 SQLite 絕對路徑,於 build 階段重建;
#   無需另設資料庫服務。若未來改用 PostgreSQL,只需於後台注入 postgresql://... 即可零修改切換。
DATABASE_URL=sqlite:///./palworld.db
