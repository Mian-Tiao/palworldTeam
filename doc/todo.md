# 開發待辦清單

> 依據:doc/requirements.md、doc/architecture.md、doc/data-model.md(均為 2026-07-16 版)
> 狀態說明:Todo / In Progress / Done
> 規則:涉及「待確認事項」的需求不得直接開發,列於文末「待確認後才可開發」區塊

## 待辦項目

### [T-1] 專案初始化
- 狀態:Done(2026-07-16)
- 優先級:高
- 對應需求:—(基礎建設)
- 相依:無
- 描述:初始化 Git;依 AGENTS.md 建立 backend/、frontend/ 目錄結構;FastAPI 與 React(Vite)腳手架;建立 `.env.example`(含 `DATABASE_URL` 說明)
- 驗收條件:
  - [x] `git init` 完成,首個 commit 包含文件與腳手架,無任何機密入版控
  - [x] 本地可同時啟動後端(uvicorn)與前端(Vite),前端能呼叫 `/api/health` 取得回應(頁面顯示「後端 API 狀態:正常」)
  - [x] `.env.example` 存在且 `.gitignore` 涵蓋 `.env`

### [T-2] 資料源評估與選定(解決 Q-3、Q-D1、Q-D2)
- 狀態:Done(2026-07-16)
- 優先級:高
- 對應需求:FR-1、FR-6
- 相依:無(可與 T-1 平行)
- 描述:評估社群資料源(GitHub 社群 JSON 資料集、Paldb.cc 等):資料完整度(種族值/技能/夥伴技能/工作適性/繁中譯名)、格式、使用條款;整理比較後**提請使用者確認**,結果記入 project-memory
- 驗收條件:
  - [x] 評估 3 個候選資料源(mlg404、blaynem/paldex、Paldb.cc)並列出比較
  - [x] 使用者確認:blaynem/paldex zh-Hant 現成資料起步,之後解包遊戲檔案更新(Q-3 結案);資料已入 backend/data/source/
  - [x] Q-D1(以 pal_dev_name 識別)與 Q-D2(克制表內建+夥伴技能人工 overlay)定案,見 project-memory

### [T-3] 傷害公式考據(解決 Q-6、Q-D3)
- 狀態:Done(2026-07-16)
- 優先級:高
- 對應需求:FR-4、FR-5
- 相依:無(可與 T-1、T-2 平行)
- 描述:蒐集社群對幻獸帕魯傷害公式的考據(含屬性克制疊加方式、等級成長、敵方防禦是否參與),整理成候選公式**提請使用者確認**採用版本,記入 project-memory
- 驗收條件:
  - [x] 公式版本經使用者確認(Q-6 結案):社群共識版含等級項、克制 2×/0.5×、STAB 1.2×,完整規格見 project-memory「已確認的業務規則(傷害公式)」
  - [x] 頭目防禦/等級參與計算定案(Q-D3 結案):指定頭目用其種族值換算,未指定時防禦為常數

### [T-4] 資料庫模型與遷移
- 狀態:Done(2026-07-16)
- 優先級:高
- 對應需求:FR-1
- 相依:T-1
- 描述:依 doc/data-model.md 建立全部 SQLAlchemy 模型與 Alembic 初始遷移(partner_skill 加成欄位先依暫定方案,T-2 定案後如需調整走新遷移)
- 驗收條件:
  - [x] 遷移可在 SQLite 執行成功且可回滾(upgrade head → downgrade base → upgrade head 驗證通過)
  - [x] 模型與 data-model.md 第 2 節逐實體一致(12 表;partner_skill buff_* 暫定欄位已於模型 docstring 標註)

### [T-5] 開發用最小種子資料
- 狀態:Done(2026-07-16)
- 優先級:高
- 對應需求:FR-1(開發支撐)
- 相依:T-4
- 描述:手工建立小規模但結構完整的種子資料(約 10 隻帕魯、9 屬性、完整 81 筆克制表、數個技能與加成型夥伴技能),供正式匯入腳本完成前的開發與測試使用
- 驗收條件:
  - [x] 一鍵腳本可灌入 SQLite(`backend/scripts/seed_dev.py`,整批替換、可重複執行)
  - [x] 涵蓋:雙屬性帕魯 3 隻、team_buff 7 隻與 riding 2 隻夥伴技能、技能習得等級 1~50(12 帕魯、48 技能、81 筆克制表)

### [T-6] 帕魯資料 API
- 狀態:Done(2026-07-16)
- 優先級:高
- 對應需求:FR-1、FR-2
- 相依:T-4、T-5
- 描述:`GET /api/pals`(search/element 篩選)、`GET /api/pals/{id}`、`GET /api/elements`、`GET /api/health`;啟動時記憶體快取;統一回應與錯誤格式
- 驗收條件:
  - [x] 各端點回傳符合 AGENTS.md 格式,錯誤訊息為繁中(統一 exception handler,500 不洩漏堆疊)
  - [x] 搜尋與屬性篩選正確;pytest 15 例通過,覆蓋成功/404/422/空結果(TC-001~004、TC-022);本地 uvicorn 實測通過

### [T-7] 前端基礎與帕魯選擇頁
- 狀態:Done(2026-07-16)
- 優先級:高
- 對應需求:FR-2
- 相依:T-1、T-6
- 描述:TanStack Query 與 Tailwind 設定;帕魯清單(搜尋、屬性篩選)、選擇喜愛帕魯為固定成員(上限暫依 Q-1 建議值 5,做成常數);純文字+屬性色塊呈現
- 驗收條件:
  - [x] 可搜尋並選取/取消固定成員,選取狀態清楚可見(卡片綠框+✓、面板 chips 可移除、達上限時其餘卡片停用;瀏覽器實測通過)
  - [x] 載入與錯誤狀態有對應畫面(載入文字、繁中錯誤+重試按鈕;停後端實測錯誤畫面與重試恢復)

### [T-8] 條件設定 UI(屬性組合目標)
- 狀態:Done(2026-07-18)
- 優先級:中
- 對應需求:FR-3(屬性組合模式)
- 相依:T-7
- 描述:目標敵人的「屬性組合」選擇(可不選=通用計算);頭目清單模式卡在 Q-2(P-4);等級選擇元件已一併實作,預設 50、範圍 1~60 為暫定值(Q-4b 未結案,定案後改 constants.js)
- 驗收條件:
  - [x] 可選 0~2 個敵方屬性(達 2 個時其餘停用),可留空=通用計算
  - [x] 選擇結果正確帶入推薦請求(瀏覽器實測:火+暗 → meta.target_elements 正確)

### [P-6] 結果呈現頁(排序+傷害拆解展開)
- 狀態:Done(2026-07-18)
- 優先級:高
- 對應需求:FR-5
- 相依:P-2、P-3
- 描述:推薦結果依總傷害排序呈現;每隊顯示生效加成 chips、成員列(★固定成員、屬性色塊、加成%、DPS);點成員展開逐技能拆解表(威力/類型/冷卻/攻擊值含基礎值/克制/STAB/單發/DPS)
- 驗收條件:
  - [x] 排序正確、固定成員標示清楚;載入(計算中…)與錯誤(繁中+重試)畫面齊備
  - [x] 拆解可手驗:瀏覽器實測 水靈兒 3 技能 DPS 合計 = 成員總輸出 624.2,克制/STAB/加成值皆正確

### [P-1] 正式資料匯入腳本
- 狀態:Done(2026-07-16)
- 優先級:高
- 對應需求:FR-1、FR-6
- 相依:T-4(2026-07-16 由「待確認後才可開發」解鎖轉入)
- 描述:全量匯入資料源全部可用一般帕魯(佔位條目與無技能者排除)、克制表、技能、夥伴技能(加成值走人工 overlay 檔 partner_skill_buffs.json)、工作適性;整批替換+import_log;seed_dev 改為同一匯入邏輯的 12 隻子集
- 驗收條件:
  - [x] 一鍵匯入 136 隻(排除佔位/無技能 18 筆),data-model 第 4 節驗證通過;前端實測顯示 136 隻
  - [x] import_log 記錄來源與版本;pytest 15 例全數通過(seed 改走同一匯入邏輯)

### [P-2] 傷害計算器
- 狀態:Done(2026-07-16)
- 優先級:高
- 對應需求:FR-4、FR-5
- 相依:T-3(公式定案;2026-07-16 由「待確認後才可開發」解鎖轉入)
- 描述:services/damage.py 純函式實作 project-memory「已確認的業務規則(傷害公式)」全部規格:攻擊值、克制/STAB 倍率、習得等級過濾、加權 DPS 取前 3、team_buff 加成、頭目/通用防禦;輸出含逐技能拆解(P-6 透明可驗證的基礎)
- 驗收條件:
  - [x] 全部公式項有單元測試(24 例),含完整公式手算對照(1513.16)
  - [x] 不依賴 FastAPI 與資料庫,輸入輸出皆為 dataclass(AGENTS.md services 規範)

### [P-3] 推薦引擎與推薦 API
- 狀態:Done(2026-07-16)
- 優先級:高
- 對應需求:FR-2、FR-4、FR-5
- 相依:P-2、Q-1(上限 5,2026-07-16 結案)
- 描述:services/recommend.py 純函式推薦引擎(固定成員必入隊、補滿至 5、總傷害由高到低);夥伴技能組合以「加成提供者子集枚舉+其餘名額最優填充」求全域最優;`POST /api/team-recommendations` 端點與 Pydantic 驗證(等級範圍暫依 Q-4b 建議 1~60)
- 驗收條件:
  - [x] 每隊皆含全部固定成員、隊伍人數=5、依總傷害排序(TC-005)
  - [x] 回應含逐成員逐技能拆解,成員合計=隊伍總傷害(TC-006);136 隻全量實測 27ms
  - [x] pytest 覆蓋引擎邏輯與 API 成功/422 情境(引擎 6 例+API 9 例,全套件 54 例通過)

### [P-8] 新版遊戲資料轉換與重匯(1.0 版)
- 狀態:Done(2026-07-18)
- 優先級:高
- 對應需求:FR-1、FR-6
- 相依:無(raw_v1 已就緒)
- 描述:寫轉換腳本把 backend/data/source/raw_v1/(遊戲 1.0 解包)轉成匯入格式,取代 2024-01 舊資料;同時修正「無技能即排除」誤殺純輔助帕魯的規則(趴趴鯰案例);讓真實加成值取代 +10% 佔位值;疊層型夥伴技能以「實際上限×維持係數」估期望加成
- 驗收條件:
  - [x] convert_raw.py 產出 pals.json,import_data.py 全量匯入 299 隻,繁中名稱正確(名稱表大小寫不一致與 Override 已處理)
  - [x] 波魯傑克斯正確參與推薦:實測作為固定成員 → 引擎組出雷系綜效隊、其疊層加成(+15%)與伏特喵(+15%)並存;趴趴鯰等純輔助已入庫
  - [x] 加成值取自遊戲被動表實值;疊層型依真實上限估期望值(焰煌 5 層×5% 從錯誤的 +50% 修正為上限內 +10%);pytest 55 例通過
- 備註:非攻擊型加成(防禦/移速)、依隊友屬性/特定帕魯條件的加成(如女皇蜂需騎士蜂在隊)尚未建模——條件型目前無條件套用,可能略高估,見 [P-10];推薦端點於 299 隻候選時約 1 秒(仍在「數秒」驗收內)

### [P-9] 夥伴技能星級選擇器
- 狀態:Done(2026-07-18)
- 優先級:高
- 對應需求:FR-4、FR-5(使用者回報波魯傑克斯加成與滿星印象不符)
- 相依:P-8
- 描述:夥伴技能加成隨專注(濃縮)星級變動;逐星值(0~4 星)自遊戲被動表萃取存 partner_skill_buff_tier;推薦 API 加 star_level 參數(全隊套用,預設滿星),前端加星級選擇器;疊層型改以滿疊理論值計並標註
- 驗收條件:
  - [x] convert_raw 萃取逐星值、新增 partner_skill_buff_tier 表與遷移(up/down 驗證)
  - [x] API star_level 生效;瀏覽器實測 波魯傑克斯 無星 +30% ↔ 4星 +150%(對應遊戲滿星),chip 標「滿疊30層理論值」;pytest 56 例通過

### [P-11] 出門活動夥伴技能總表
- 狀態:Done(2026-07-18)
- 優先級:中
- 對應需求:延伸功能(使用者提案)
- 相依:P-8(解包資料)、P-9(逐星級)
- 描述:列出釣魚/挖礦/伐木/採集/搬運的加成型夥伴技能(不做最佳化——釣魚有失敗風險,收益與穩定無法用單一分數排,交玩家判斷)。每項標「收益/穩定」、每隻可各自選星級(預設滿星)。機動類使用者定案不收;基地打工歸第二階段
- 驗收條件:
  - [x] app/core/activities 對照表 + convert_raw 萃取(含多效果,如森猛獁伐木+挖礦)+ partner_skill_activity_effect 表與遷移
  - [x] GET /api/activity-skills(收益優先排序);前端新分頁,每隻星級下拉即時改值
  - [x] 瀏覽器實測:釣魚 10 項收益/穩定分開、桃晶鯨/墨沫姬正確標穩定、海月仙 4★+95%↔無星+55%;pytest 62 例通過

### [P-10] 條件型與非攻擊型夥伴技能建模(延伸)
- 狀態:Todo(2026-07-18 建立)
- 優先級:低
- 描述:依隊友屬性/特定帕魯在隊才生效的加成(女皇蜂↔騎士蜂等)目前無條件套用,可能高估;非攻擊型(防禦/移速)未納入。需在 partner_buff 萃取時讀取被動的 TargetTribeIds / 條件參數並於引擎判定
- 驗收條件:
  - [ ] 條件型加成僅在條件滿足時計入
  - [ ] 明確定義哪些非攻擊型要納入(或維持不納入)

### [P-12] 1.0 純被動相對輸出模型
- 狀態:Done(2026-08-13)
- 優先級:高
- 對應需求:FR-4、FR-5
- 相依:P-8(1.0 解包資料)
- 描述:依使用者目前目標,把推薦收斂為「固定選中的帕魯在純被動隊伍支援下達到最高相對輸出」;更新 1.0 屬性倍率、攻擊值來源與計算模型,並避免把理論分數標示成實機精確 DPS
- 驗收條件:
  - [x] 所有主動技能統一使用帕魯顯示攻擊(shot_attack_stat);category 僅保留作技能行為/顯示資料
  - [x] 克制/抵抗倍率改為 1.50/0.66,雙屬性逐項相乘;STAB 維持 1.20
  - [x] 相對輸出採 1.0 比較模型,只套用 pal_attack 被動夥伴加成;不納入騎乘、主動發動、玩家武器或條件觸發
  - [x] 前端明示「輸出分數」與模型限制;pytest 65 例、前端 lint/build 通過

### [P-13] 命中疊層被動納入推薦
- 狀態:Done(2026-08-13)
- 優先級:高
- 對應需求:FR-4、FR-5
- 相依:P-12
- 描述:命中即可累積的攻擊被動(如波魯傑克斯)在實戰可穩定疊滿,改按最大層數換算並納入推薦;擊殺疊層仍依單一頭目情境保守計 1 層
- 驗收條件:
  - [x] BulletHit stack 依實際 cap 滿層換算;波魯傑克斯 0★/4★ 為 +30%/+150%
  - [x] 推薦 API 納入 flat 與 stack 的 pal_attack 加成,仍排除 player_attack/騎乘/主動效果
  - [x] 前端明示疊層已換算;pytest 65 例、前端 lint/build、實際 API 驗證通過

### [P-14] 推薦成員被動說明
- 狀態:Done(2026-08-14)
- 優先級:中
- 對應需求:FR-5
- 相依:P-12、P-13
- 描述:點擊推薦隊伍成員時,在技能分數前顯示該帕魯的夥伴技能/隊伍被動、當前星級數值、是否納入模型,以及實際影響哪些固定帕魯
- 驗收條件:
  - [x] 推薦 API 的每位成員包含結構化 partner_skill 與 applies_to_fixed_pal_names
  - [x] 展開成員可辨識已計入/未計入、flat/stack 與疊層上限
  - [x] 不把隨機個體被動誤稱為系統已知資料;pytest 66 例、前端 lint/build、瀏覽器驗證通過

### [P-15] 被動描述星級數值一致性
- 狀態:Done(2026-08-14)
- 優先級:高
- 對應需求:FR-5
- 相依:P-14
- 描述:遊戲原始夥伴技能文字由專注1階(前端無星)模板產生,與使用者所選星級可能矛盾,引用被動的模板值甚至可能留空;推薦結果改以結構化 current_buff_value 為唯一數值來源,原始文字只描述觸發機制
- 驗收條件:
  - [x] 波魯傑克斯選4星時不再於機制文字顯示每層1%,以上方+150%為準
  - [x] 織夜鹿等引用被動不再出現「提升%」缺值文字
  - [x] 畫面明示機制文字來自無星模板,數值以上方目前星級為準;pytest 66 例、前端 lint/build、兩案例瀏覽器驗證通過

### [P-16] 產品轉向:加成速查列表取代隊伍推薦
- 狀態:Done(2026-08-16)
- 優先級:高
- 對應需求:FR-2、FR-4、FR-5(重新詮釋)
- 相依:P-12~P-15(沿用其資料層與星級模型)
- 背景(資料佐證,2026-08-16 實測;**數字已於同日修正,見下方 P-17**):
  - 全遊戲 `pal_attack` 常駐加成 **14 隻**:通用 3(波魯傑克斯150%/織夜鹿80%/焰煌10%)+ 各屬性 1(無屬性與暗屬性各 2)
  - 固定 1 隻(非 buffer)時:7 個屬性適用 4 隻、空位 4 個 → **答案唯一**;無屬性與暗屬性適用 5 隻 → 需取捨
  - 實測 10 支推薦隊伍的 `fixed_total_dps` 完全相同(差距 0.0%),差別只在對固定帕魯零貢獻的填充位
  - 固定 2 隻(異屬)差距 2.7%;固定 3 隻(異屬)差距 **22.2%** → 只有固定成員多時才存在真正的取捨
  - 結論:多數情況下「推薦」是在解一個不存在的最佳化問題,10 筆結果是填充者排列組合的灌水
- 決策理由:加成百分比**直接來自遊戲被動表(事實,玩家可對照驗證)**;輸出分數是**我們合成的模型(多段命中、動畫時間、命中率、引擎減傷皆未建模,必然對不上實機)**。把可驗證的事實當主角、把估計降級,才守得住工具可信度
- 描述:
  1. **主畫面改為加成矩陣**:列=可用 buffer 帕魯,欄=使用者的固定帕魯,格=該 buffer 對該帕魯的加成%(依目前星級);同時顯示各固定帕魯的攻擊種族值供使用者自行權衡
  2. **排序不使用傷害模型**:依「加成幅度 × 影響到的固定帕魯數量」排序,天然解決混屬性排名問題
  3. **動態摘要**:空位 ≥ 適用 buffer 數 → 明示「全帶即可,無需取捨」;空位 < 適用數 → 明示「N 隻搶 M 個空位,選擇影響約 X%」
  4. **傷害模型降級不刪除**:`services/damage.py`、`recommend.py` 保留,但不再是主畫面主角;如需顯示須明確標示「粗略估計,僅供同條件比較」
  5. **文案定位調整**:由「配隊最佳化/傷害計算」改為「夥伴技能加成速查」,與既有「活動夥伴技能」分頁形成一致的產品(戰鬥加成 / 活動加成兩張速查表)
  6. 同一被動 ID 不同帕魯(姬小兔/米露菲)**仍各自計算**(2026-08-16 使用者確認:不可疊加是依帕魯編號/種族,同種族帶多隻才不疊;引擎候選為每種一筆,結構上不可能違反)
- 驗收條件:
  - [x] 矩陣正確標示每個 buffer 對每隻固定帕魯是否生效——生效判斷抽成 `damage.buff_applies_to`,矩陣與傷害計算**同源**,並有測試斷言兩者一致
  - [x] 排序僅使用加成值與影響隻數(`sort_score = buff_value × affected_count`),不 import `damage` 的輸出分數
  - [x] 摘要以「需招募數 vs 空位」判定;固定成員自身提供的加成不佔空位(啼卡爾案例:需招募 3/空位 4 → 全帶即可)
  - [x] 主畫面改為加成矩陣;輸出估算降級為「輸出估算(實驗性)」次要按鈕
  - [x] pytest 78 例(新增 12)、前端 lint/build 乾淨、桌機/手機瀏覽器實測無 console 錯誤
- 實作:`services/buff_matrix.py`(純函式)、`api/buff_matrix.py`(`POST /api/buff-matrix`)、
  `schemas/buff_matrix.py`、前端 `api/buffMatrix.js` + `components/BuffMatrix.jsx`(選取即時更新,免按鈕)
- 收尾(2026-08-16 完成):
  - [x] 文案定位:標題/頁籤/README 改為「幻獸帕魯夥伴技能速查」;分頁改「戰鬥加成 / 活動夥伴技能」;
    輸出估算於 README 明確標為實驗性且非實機傷害
  - [x] 清單卡「攻擊」改用 `shot_attack_stat`(= 遊戲顯示攻擊,與模型同源),修正先前 `max(shot, melee)`
    造成的 123 隻不一致(炎魔羊 150→100);加回歸測試逐隻比對清單值與詳情的 `shot_attack_stat`

### [P-17] 資料完整性修正:圖鑑編號 -1 的帕魯被漏掉
- 狀態:Done(2026-08-16)
- 優先級:高
- 對應需求:FR-1
- 相依:P-8
- 背景:使用者提供官方圖鑑截圖比對,發現**惡魔眼**(暗屬性帕魯攻擊 +15~30%)不在資料中
- 根因:`convert_raw.py` 以 `ZukanIndex > 0` 判定可玩性,但屋久島內容的正規帕魯圖鑑編號為 **-1**,整批被排除
- 修正:
  1. 可玩性改由「`IsPal` 且非 `IsBoss` 且非變種條目 且有正常中文名」判定,不再用 ZukanIndex
  2. `VARIANT_MARKERS` 補上 `BOSS_`/`GYM_`/`POLICE_`/`PREDATOR_`,擋掉 BossRush 等重複實體(這些條目 `IsBoss=False`,原本會混入)
  3. 佔位名稱(`zh_Hant_Text`)改在轉換階段就濾除,使 pals.json 與資料庫筆數一致
- 影響:匯入 287 → **298 隻**(新增屋久島 11 隻);`pal_attack` 加成 13 → **14 隻**
- **連帶修正先前結論**:暗屬性適用加成由 4 → **5 隻**,與無屬性同樣需要取捨(P-16 背景已更新)
- 驗收條件:
  - [x] 惡魔眼(`YakushimaBoss001_Small`)正確匯入並出現在加成矩陣
  - [x] 以官方圖鑑截圖建立回歸測試:11 隻屬性限定加成全部抓到且皆為 0★15%→4★30%
  - [x] pals.json 與資料庫筆數一致(298);pytest 80 例通過

### [P-18] 條件型加成:克制增傷
- 狀態:Done(2026-08-16)
- 優先級:中
- 對應需求:FR-4、FR-5
- 相依:P-16、P-17
- 描述:遊戲有 8 隻提供「克制增傷」的帕魯(`ElementBoostWeakness_<屬性>`,0★25%→4★40%,各配一個屬性),原本完全未納入。因其有觸發條件,**刻意與常駐加成分開**呈現,不併入輸出模型
- 8 隻與生效條件:粉粉布偶鯊(火→草/冰)、金盔仔(地→雷)、閃丸王(水→火)、雷角馬(雷→水)、艾小兔(草→地)、川霜龍(冰→龍)、歿殃(龍→暗)、極道蛙(暗→無)
- **採用的解讀(假設)**:限定該屬性——「以該屬性攻擊剋制的敵人」才生效,非泛用弱點加成。
  依據:8 隻各有獨立且帶屬性的 effect ID;若為泛用則不需拆成 8 個。遊戲描述文字為簡化模板
  (「命中敵方弱點時」未提屬性),但本專案已於 P-15 驗證描述模板不可靠,故採信結構化資料。
  **若日後實測為泛用,只需改 `build_conditional_buffs` 的 `effective_vs` 判定**
- 實作:`convert_raw.extract_weakness_buff`(獨立於 partner_buff)、`partner_skill.weakness_element_id`
  + `partner_skill_weakness_tier` 表與遷移、`buff_matrix` 服務加 `build_conditional_buffs`、
  `/api/buff-matrix` 接受 `target_elements`、前端獨立「條件型加成」區塊
- 驗收條件:
  - [x] 8 隻全部抓到,0★25%→4★40%;`effective_vs` 由克制表推導(火 → 草、冰 兩個)
  - [x] 呈現改為**依已選帕魯的屬性對應**(選暗屬帕魯→只顯示暗屬克制增傷極道蛙,標「給 <帕魯名>」);
        未選帕魯時收合,可按「全部 8 隻」瀏覽。相關者排最前,目標屬性仍可作次要生效標示
  - [x] 條件型**不混入**常駐加成矩陣(有測試斷言);不進入 `attack_buff_rate`,輸出模型不受影響
  - [x] pytest 86 例通過;前端 lint/build 乾淨;桌機/手機瀏覽器實測無 console 錯誤、無溢出

### [T-9] 部署設定(Render)
- 狀態:Done(2026-09-12);已上線 https://palworld-partner-skill-finder.onrender.com
- 優先級:中
- 對應需求:成功標準 3(朋友可用網址直接使用)
- 相依:T-6
- 描述:改採 **Render Docker web service(免費層)+ SQLite 於 build 階段重建**(取代原訂 PostgreSQL,見 project-memory 2026-09-12)。多階段 Dockerfile:Node 打包前端 → Python 執行後端,單一服務同供 API 與靜態前端
- 產出檔案:`Dockerfile`(多階段)、`.dockerignore`、`render.yaml`(Blueprint)
- 本機驗證:全新 SQLite 跑 `alembic upgrade head` + `import_data.py` 成功(298 帕魯 / 8 克制增傷)
- 驗收條件:
  - [x] build 流程含前端打包與 Alembic 遷移(Dockerfile 多階段;本機 dry-run 通過)
  - [x] 無任何機密出現在程式碼或設定檔(SQLite 無連線密碼;`.env` 仍在 .gitignore)
  - [x] 正式網址可開啟前端並成功呼叫 `/api/health`(已驗證:298 帕魯載入、加成矩陣與克制增傷正常、零 console 錯誤)

### [T-10] 推薦端點速率限制
- 狀態:Todo
- 優先級:低
- 對應需求:架構第 7 節(基本濫用防護)
- 相依:P-3(推薦 API 完成後)
- 描述:以 slowapi 對 `POST /api/team-recommendations` 加簡單速率限制
- 驗收條件:
  - [ ] 超過限制回 429 與繁中錯誤訊息

## 待確認後才可開發

| 編號 | 項目 | 待確認事項 | 來源 | 解鎖方式 |
|------|------|-----------|------|----------|
| P-1 | 正式資料匯入腳本(全量替換+import_log) | **已完成(2026-07-16)**,見上方 [P-1] 條目 | requirements / data-model | — |
| P-2 | 傷害計算器(services/,含單元測試) | **已完成(2026-07-16)**,見上方 [P-2] 條目 | requirements / architecture | — |
| P-3 | 推薦引擎與 `POST /api/team-recommendations` | **已解鎖(2026-07-16)**:Q-1 結案(上限 5)、P-2 完成 | requirements | 開發中,見 [P-3] 條目 |
| P-4 | 頭目清單資料與頭目選擇 UI | Q-2(初版收錄範圍;資料集含塔主條目可直接取用) | requirements / architecture | 使用者確認收錄清單 |
| P-5 | 等級選擇元件 | **已完成(2026-07-18)**:Q-4b 結案,範圍 1~80、預設 80(使用者確認遊戲上限 80) | requirements | — |
| P-6 | 結果呈現頁(排序+傷害拆解展開) | **已完成(2026-07-18)**,見上方 [P-6] 條目 | requirements FR-5 | — |
| P-7 | 打工配置(第二階段全部功能) | Q-7、Q-D4(需求細節未訪談) | requirements FR-7 | 第二階段開發前補訪談 |
